**JavaScript学习**

# 1、了解JS

## 1.1、是什么

是一种运行在客户端（浏览器）的编程语言，实现人机交互效果。

是一种解释型语言，运行的时候即时的解释，Java式编译型语言，在执行前进行编译，生成中间代码文件。

## 1.2、可以干什么

1. 网页特效 (监听用户的一些行为让网页作出对应的反馈) 
2. 表单验证 (针对表单数据的合法性进行判断) 
3. 数据交互 (获取后台的数据, 渲染到前端) 
4. 服务端编程 (node.js) 

## 1.3、组成

1. ECMAScript:
   - 规定了js基础语法核心知识。 比如：变量、分支语句、循环语句、对象等等
2. Web APIs :
   -  DOM 操作文档，比如对页面元素进行移动、大小、添加删除等操作 
   -  BOM 操作浏览器，比如页面弹窗，检测窗口宽度、存储数据到浏览器等等

# 2、JS代码规范与语法

## 2.1、JS书写的位置

1. 内部JS
   - 直接写在html文件里，用script标签包住 
   - 规范：script标签写在上面 
   - 拓展： alert(‘你好，js’) 页面弹出警告对话框
2. 外部JS
   - 代码写在以.js结尾的文件里 
   - 语法：通过script标签，引入到html页面中。
3. 嵌入式JS
   - 直接写在html文件里，用script标签包住 
   - 规范：script标签写在上面 
   - 拓展： alert(‘你好，js’) 页面弹出警告对话框

![](asset/H1.png)

## 2.2、与c语言相同的语法

### 2.2.1、注释

### 2.2.2、变量的赋值、命名规范

### 2.2.3、运算符、比较符

### 2.2.4、逻辑语句

### 2.2.5、函数传参

## 2.3、与c语言不相同的语法

### 2.3.1、输入输出语法

- 输出

  ```
  alert(msg)//浏览器弹出警示框
  ```

- 打印到后台

  ```
  console.log(msg)//浏览器控制台打印输出信息
  ```

- 输入

  ```
  prompt(into)//浏览器弹出输入框，用户可以输入
  ```

### 2.3.2、变量

- 声明

  ```
  var 变量名;
  let 变量名;
  //两者的区别，let相比var更严谨。不允许在变量创建前调用这个变量。不允许变量重复定义。以及解决一些变量提升、全局变量、没有区级作用域等等。
  ```

- 变量的数据类型是变化的，其他操作与c语言一致

- 有数据类型但是可以自由赋值

  ![](asset/H2.png)

- 数字前面加0表示八进制0x表示16进制

- 数字有最大值和最小值

  ```
  Number.MAX_VALUE
  Number.MIN_VALUE
  ```

- 无穷大与无穷小以及非数值

  ```
  Infinity
  -Infinity
  NaN
  ```

### 2.3.3、判断是否为数字

```
isNaN()
```

### 2.3.4、字符串的长度

```
var str='my';
var len=str.length
```

### 2.3.5、字符串的拼接

- 用'+'号拼接

### 2.3.6、输出变量类型

```
var num=10;
console.log(typeof num);
```

**注意:**在控制台日志中的数据蓝色是数字型、深蓝色是bool、黑色是字符型、undefined和null为浅灰色

### 2.3.6、数据类型转换

一般用于prompt接受到数据后进行转换

- 转换为字符串

  ```
  toString();
  String();//强制转换
  /////////////////用法/////////////////
  var num=10;
  var str=num.toString();
  var str1=String(num);
  ```

- 转换为数字型

  ```
  parseInt(string);
  parseFloat(string);
  Number();//强转
  //JS隐式转换 - * /
  var str='12';
  var num=str-0;//或者
  var num1=str-'10';
  ```

- 转换为布尔型

  ```
  Boolean();
  //只要是有内容的都会转为ture
  //0、''、NaN、null、undefined
  ```

  ### 2.3.7、比较运算符

- === 两边类型和数值都相等
- !==
- 字符串比较，是比较字符对于的ASCII码
- NaN不等于任何值，包括他本身
- 不同类型可以比较
  - 会隐式成number类型再比较

### 2.3.8、数组

- 有类似容器操作
  - arr.push()//加到末尾
  - arr.unshift()//加到开头
  - arr.pop()//删除最后并返回这元素
  - arr.shift()//删第一个返回
  - arr.splice()//操作下标，arr.splice(start,deleteCount)

### 2.3.9、函数

```
function 函数名(){

}
```

不需要定义函数类型能直接用return返回参数

- 没用的匿名函数

  ```
  let fn =function(){
  
  }
  fn();
  ```

- 立即执行函数

  ```
  (function (){console.log()})();
  (function (){console.log()}());
  //多个立即执行函数要用 ; 隔开，要不然会报错
  //防止变量污染
  //无需调用，立即执行，其实本质已经调用了
  
  ```

### 2.3.10、对象

- 声明方式

  ```
  let 对象名={
       属性名：值
       功能名：函数
  }
  ```

  ```
  遍历对象
  for(let k in 对象名)
  {
   console.log(k);
   console.log(对象名[k])
  }
  ```

Javascript中有很多内置函数相当于函数库。

### 2.3.11、数据类型

简单类型又叫做基本数据类型或者值类型，复杂类型又叫做引用类型。 

- 值类型：简单数据类型/基本数据类型，在存储时变量中存储的是值本身，因此叫做值类型 string ，number，boolean，undefined，null 
- 引用类型：复杂数据类型，在存储时变量中存储的仅仅是地址（引用），因此叫做引用数据类型 通过 new 关键字创建的对象（系统对象、自定义对象），如 Object、Array、Date等

- 栈（操作系统）：由操作系统自动分配释放存放函数的参数值、局部变量的值等。其操作方式类似于数据结构中 的栈； 
  - 简单数据类型存放到栈里面 
- 堆（操作系统）：存储复杂类型(对象)，一般由程序员分配释放，若程序员不释放，由垃圾回收机制回收。 
  - 引用数据类型存放到堆里面